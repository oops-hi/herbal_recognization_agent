# 人机交互记录

> 本文件记录使用过程中发现的问题、AI 的纠错与解决过程，供答辩/论文复盘。
> 格式：`## N. 一句话问题（日期）` → 现象 / 原因 / 解决 / 验证 / 教训

## 1. AI 在前端对返回的 markdown 不渲染（2026-08-27）

**现象**：模型回答含 `**加粗**`、`- 列表`、`## 标题` 等 markdown 标记，前端对话框原样显示语法符号。

**原因**：`static/js/main.js` 的 `addMsg()` 用 `bubble.textContent = text` 插入回答 —— `textContent` 按纯文本处理，不做任何 markdown 解析。

**解决**：
1. 新增 `static/js/markdown.js` —— 手写迷你渲染器，**离线零依赖、不走 CDN**（答辩教室断网约束，pyvis 同款教训）；覆盖加粗/斜体/行内代码/代码块/标题/列表/引用/段落
2. 三层处理保证正确与安全：先 HTML 转义（防 XSS）→ 块级解析（列表合并、代码块保护）→ 行内解析（行内代码占位保护，防止内部 `**` 被误解析）
3. 仅对**智能体回答**开启渲染；用户消息/系统提示/错误保持纯文本
4. 补气泡内样式（`ul` 缩进、`code` 底色、引用线）
5. **重启服务** —— Flask 非 debug 模式模板有缓存，新增 `<script>` 标签不重启不生效

**验证**：node 单测覆盖全部语法 + `<script>` 注入样例，输出全部正确；页面 Ctrl+F5 后正常显示格式化回答。

**教训**：插入内容前先想清楚 `textContent`（纯文本）vs `innerHTML`（可渲染）的语义差异；改模板后记得服务端有模板缓存。

## 2. 交互图谱节点拖不动（2026-08-27）

**现象**：D3 力导向图渲染正常，但节点无法拖拽、悬停高亮与点击联动也全部失效。

**原因**：SVG 层级错误 —— 透明背景 rect（`.graph-bg`，用于点击空白取消聚焦）被 append 在节点层 `<g id="root">` **之后**。SVG 后添加的元素在上层，`fill="transparent"` 的 rect 依然拦截全部 pointer 事件，节点永远收不到 `pointerdown`。

**解决**：背景 rect 改为先于节点层 append（`static/js/graph.js` renderForce），并加注释说明「SVG 后添加元素在上层」的坑。

**验证**：node 语法检查通过，浏览器刷新后拖拽/悬停/点击全部恢复。

**教训**：SVG 内分层必须按「底层 → 上层」顺序 append，透明 rect 不是「不参与交互」——只要它有 fill 就会挡事件。

## 3. 悬停高亮时非邻接节点瞬间消失，晃眼（2026-08-27）

**现象**：鼠标悬停节点，其他非直连节点几乎瞬间淡出（opacity 0.12），视觉冲击大。

**原因**：① `.dim` 类加在整个节点 `g` / 边上，但 CSS `transition` 只定义在 `circle/rect` 上 —— **文字标签和边没有过渡、瞬时消失**；② 0.12 透明度淡出过狠，「弱化」做成了「消失」。

**解决**：过渡改到 `g` / `.link` 本身（0.2s → 0.35s ease），淡出值 0.12 → 0.3（保留轮廓感）。

**验证**：刷新后悬停淡入淡出平滑。

**教训**：给整体元素（`<g>`）加过渡状态时，transition 必须定义在整体元素上，而不是只给内部子元素。

## 4. 图谱画布纯白底，Windows 白色鼠标指针看不见（2026-08-27）

**现象**：画布背景用 `--card`（#fffdf8 近纯白），Windows 白色鼠标指针在白底上无法辨认。

**原因**：画布底色与卡片色相同，未考虑浅色主题下白色指针的对比度。

**解决**：画布底色改为暖米色 `#f0eadf`（比卡片深一档，与边框 `--border` 协调）；节点文字描边 `stroke` 同步改为画布色，避免文字周围白圈突兀。

**验证**：刷新后指针清晰可见，画布与页面米色主题仍统一。

**教训**：浅色画布不能取纯白；交互画布底色要考虑鼠标指针对比度。

## 5. 进入图谱页短暂闪现 v1 静态图（2026-08-27）

**现象**：从首页点进 `/graph`，先显示 v1 的 matplotlib 静态 PNG，约几百毫秒后才切换成交互力导向图。

**原因**：渐进增强顺序设计反了 —— 静态 PNG 默认可见（无 JS 兜底），交互图要等 `/api/graph` fetch 成功后才替换。

**解决**：改为交互图（SVG +「图谱加载中…」提示）作默认首屏，静态 PNG 默认 `display:none`，仅 `/api/graph` 失败（断网/后端挂）时降级显示 + 提示条。另注意：**Flask debug=False 时模板有缓存，改 graph.html 后必须重启服务**（与第 1 条同款教训）。

**验证**：重启后进入页面直接是加载中 → 交互图，无 v1 闪现；断网降级路径仍可用。

**教训**：降级兜底和默认态不要共用同一个元素默认可见；「异常才显示的兜底」应默认隐藏、失败时显式打开。

## 6. 桌面版 Vue 迁移遗留：图谱查询/识别卡不显示 + 工具链不逐条弹出（2026-08-28）

**现象**：Vue3+Electron 桌面版（vue-electron-exe 分支，`python app.py` + 浏览器同现）：
① 图谱页搜索框/点节点查询「无反应」；② 首页识别卡、补拍建议、上传错误提示、断网降级静态图全部看不见；
③ 对话工具调用链不逐条弹出，且全部堆在消息流末尾、无法折叠。

**原因**（三个独立 bug，均为 Vue 迁移遗留）：
1. **CSS 残留 `display:none`**：旧 Jinja 版用 JS 手动 toggle class 控制显隐，样式里默认 `display:none`。Vue 版改用 `v-if` 控制后没删，`v-if` 把元素加进 DOM、CSS 又强制隐藏 → 永远看不见。波及 `.search-result`（图谱查询结果）、`.rec-card`（识别卡）、`.advice-box`（补拍建议）、`.error-box`（错误提示）、`#graph-static`（降级图）。
2. **Vue 响应式坑**：`const tl = { steps: [] }` 普通对象 push 进 ref 数组后被转成 reactive 代理，但原变量仍指向普通对象——之后 `tl.steps.push()` 绕过代理修改底层数组，不触发更新 → 工具链空白/不逐条。必须 `reactive({ steps: [] })` 创建。
3. **渲染顺序错误**：messages 与 timelines 分两个数组 v-for，时间轴全部堆在消息末尾；旧版是按时序插入（问题→工具链→回答）。且折叠功能缺 CSS（`data-collapsed` 无规则，点标题只改 data 属性不隐藏）。

**解决**：① 删除 5 处残留 `display:none`；② ChatPanel 的 timeline 单元改为 `reactive` 创建；③ 重构为单一有序单元流 `units[]`（msg/timeline 交错），模板 v-for 单数组渲染；补 `.timeline[data-collapsed="1"] .tl-step { display:none }`。④ 排查过程中的假线索：浏览器 Console 404 实为 `/favicon.ico`；端口 5000 曾有 4 个 app.py 实例同时监听（多次启动未关旧进程 + 一个裸 `python` 起的 base 环境），请求被分发到异常实例导致 404。

**验证**：硬刷新后图谱搜索/节点点击出档案；识别卡/建议框正常；对话工具链逐条弹出、顺序正确、可折叠；后端 SSE 用 Python requests 直测逐条到达（间隔 1.4s/6s 真实）。

**教训**：① 模板迁移后必须全局搜 `display:none` / `display: none` 核对与 v-if/v-show 的关系；② ref 数组 push 的对象若后续还要改，必须 reactive 创建；③ 流式数据按时间顺序渲染要单一有序数组，别分列表后置；④ 旧进程别留着——端口争抢 + 坏环境实例会制造"幽灵 bug"。

## 7. 上传后对话区不弹识别、拒识图不进上下文（2026-08-29）

**现象**：① 上传图片后，要手动点进对话框才看到识别提示（且拒识/域外图时对话区完全无提示）；② 识别不是药材时，后续追问没有上下文——甚至更糟：先识别枸杞子、再传一张猫图被拒后，追问『这是什么』模型仍带着「当前药材=枸杞子」的上下文作答（旧上下文残留，串药）。

**原因**：
1. 前端 `HomeView.onUpload` 只在 `status==='ok'` 时 `pushSystemNote`，拒识/模型未就绪分支不向对话区推送任何提示，且上传后不聚焦输入框；
2. 后端 `/upload` 拒识分支（non_herb / VLM none / 本地低置信）完全不写会话上下文——`current_herb` 只在成功分支写入，且拒识分支**不清除**旧值 → 会话里 `sess["messages"]` 还留着上一味药的 HERB_CTX system 消息，`stream_run` 只在 `current_herb` 为真时调用 `_set_herb_context`（替换），为 None 时旧消息原样留在历史里继续误导模型。

**解决**：
1. 后端新增「上传识别上下文」通道（`core.UPLOAD_CTX_PREFIX` + `_set_upload_context/_clear_upload_context/_clear_herb_context`）：拒识分支写 `sess["upload_ctx"]`（如实告知「判定为非中药饮片（疑似『猫科动物』）」/「置信不足」+ 补拍建议）并 `current_herb=None`；成功分支清 `upload_ctx`；`stream_run` 对 `current_herb`/`upload_ctx` 做了对称的 注入↔清除（含**历史 messages 里旧 HERB_CTX 消息的清除**，防串药）；
2. 前端：所有上传结果（ok/拒识/model_not_ready）都向对话区推 system-note，上传完成后 `focusInput()` 自动聚焦输入框——用户无需手动点击即可直接提问。

**验证**：flask test_client 端到端（零 DeepSeek 成本）：枸杞子真实图 → ok / current_herb=枸杞子；随后 1×1 黑图 → low_confidence / current_herb=None + upload_ctx 写入 ✅；core.py 三个场景离线单测（旧药材上下文清除、拒识注记注入、拒识→成功互清）全过；`npm run build` 通过。

**教训**：会话上下文必须成对管理——「写新」的同时要「清旧」，且清理要覆盖**历史 messages** 而不只是会话字段；所有分支（成功/拒识/降级）都要走同一条上下文写入路径，否则串药比没上下文更危险。

## 8. DeepSeek 400「assistant tool_calls 必须紧跟 tool 消息」（2026-08-29）

**现象**：对话中出现 `DeepSeek API 错误 400: An assistant message with 'tool_calls' must be followed by tool messages responding to each 'tool_call_id'`，对话中断。

**原因**：SSE 客户端中途断开（刷新页面/关窗口/断网）时，Flask 的 `stream_with_context` 停止消费生成器 → 生成器在 `yield` 处被 `GeneratorExit` 中断。原代码顺序是「yield tool 事件 → yield stats → 追加 tool 消息」，**assistant 消息（带 tool_calls）已写入历史、而 tool 回复还没追加**，坏历史残留到下一轮请求 → DeepSeek 严格校验（assistant tool_calls 后必须紧跟每个 tool_call_id 的 tool 回复）直接 400。

**解决**（`agent/core.py`）：
1. **两遍循环**：工具轮次改为先「执行全部工具 + 同步写入全部 tool 回复」，再「逐条 yield SSE 事件」——任何中断点历史都已完整；
2. **历史消毒兜底**：新增 `_sanitize_history()`，在每次 `_chat_completion` 发 API 前扫描历史，对「assistant 带 tool_calls 但 tool 回复缺失」的 id 补占位消息（`[系统] 该工具调用因连接中断未执行完成`），幂等；即使有其他路径产生坏历史也会自愈；
3. assistant 消息写入提前到 yield turn/stats 之前。

**验证**：离线单测 4 组——坏历史消毒补占位 ✅；两遍循环事件顺序 `turn→stats→tool→stats→tool→stats→turn→stats→answer` + 历史完整 ✅；`gen.close()` 模拟客户端断开后历史仍含全部 tool 回复 ✅；API 调用前自愈 ✅。`npm run build` 通过。

**教训**：① 生成器 + SSE 的「yield 中断」是隐形的：**凡是写入服务端状态的代码必须放在所有 yield 之前**，且要意识到「状态写入 + 事件推送」要解耦成两阶段；② 对上游 API 的严格消息结构校验，本地历史必须设消毒兜底，不能只靠正常路径保证。

## 9. 修复后仍见 400 —— 旧后端进程没重启（2026-08-29）

**现象**：修复两遍循环/消毒后，用户会话截图仍出现 DeepSeek 400「insufficient tool messages」。

**原因**：Electron 的 dev 模式由主进程 **spawn 一个 Python 后端进程**（开发者视角：后端进程在修复前就已运行）。修改磁盘上的 `agent/core.py` 只改了源码，**正在运行的后端进程内存里还是修复前的代码**——客户端不断开时旧代码看起来正常（yield 事件 → append tool 消息 都能跑完），但只要 SSE 客户端中途断开，旧代码的坏历史缺陷立刻复发。本地或新起的后端进程验证全过，恰恰反证是旧进程。

**验证**：
1. 真实 API 复现（新代码）：第 1 问「同时查菊花和甘草」→ 双工具 + 双 tool 回复 → answer 成功；第 2 问带拒识上下文「菊花可以和他一起泡水吗」→ check_compatibility → answer 成功；历史 11 条角色流自洽。
2. 消毒逻辑升级为「补缺 + id 归一」双兜底并加量测试：id 为 null/空串 → 生成 `call_<idx>` 并**双向修补**（tool_calls 与 tool 回复的 id 一一对应）——DeepSeek 强校验不接受空 id；顺带修掉一个 set 迭代序不定导致 id 错配的 bug（`list(need)[n]` → 有序 `ordered_ids[n]`）。
3. 四组离线单测（归一化/补占位/正常史不变/幂等）全过。

**教训**：改后端 Python 代码后，**Electron/dev 场景必须重启后端进程才生效**；排查「我明明修了却还犯」先问一句「进程是修复前启动的吗？」。顺带教训：编辑源码时 `old_string`/`new_string` 要逐字核对，本会话曾误把 new_string 写成 `core.py` 字符串破坏函数，靠编译+diff 及时发现。

## 10. 对话区四连 bug：图片消失 / 「卡死」无输出 / 页面滚动 / 无粘贴（2026-08-29）

**现象**（用户实测四连）：① 发送图片后图片立即消失；② 图+文「这个可以和谁配药时」→ 显示 LLM 调用 2s、无工具链、无输出、无停止按钮；③ 识别结果显示时页面整体滚动，要求两个卡片内部各自滚；④ 不支持 Ctrl+V 粘贴图片。

**原因与解决**：
1. **图片消失**：`ChatMessage.vue` 的 `safeImage` 前缀白名单只放行 `http:`/`blob:`，**漏了 server 相对路由 `/uploads/...`**（上传成功后 blob 被替换为 server URL → 被白名单过滤 → 不渲染）。补白名单。教训：**前端资源白名单必须覆盖「本服务相对路径」这一类**——只想到外部协议会漏掉自家路由。
2. **卡死**：先用后端直连全链路复现——上传真药材图（覆盆子 0.9）→ 问同样问题 → **4 轮 LLM + 5 工具调用 + answer 正常到达，0 异常**。卡死不在正常链路，而是 **LLM 挂起窗口**：DeepSeek 网络抖动/60s 超时内后端不推任何帧（yield 发生在每轮 LLM 完成后），前端无取消手段 → 用户视角「无输出且卡死」。修复三件套：前端**停止按钮**（AbortController 中止 fetch；客户端断开时服务端走 GeneratorExit，`_sanitize_history` 两遍循环保证历史完整、finally 落盘——幂等既有设计）+ **「处理中」指示**（uploading/streaming 全程可见）+ 后端 `generate()` **非预期异常兜底**：`except Exception` 必 yield error 帧（原来任何非 requests 系异常会静默掐断流——「无输出卡死」的另一个可能源头）。
3. **卡片内滚**：`body` 固定 `100vh + overflow:hidden`；`main` 加 `min-height:0`（flex 子项默认 min-height:auto 会撑破父级）+ `grid-template-rows: minmax(0,1fr)` + `overflow:hidden`；`.card` `overflow:hidden`，左卡 `.result-card` 整卡滚；chat-body `flex:1 + min-height:0`（**flex item 必须显式 min-height:0 才能收缩，否则内部 overflow-y:auto 永不生效**——经典雷）。窄屏（<900px）恢复整页滚动，两卡叠放时内部滚会别扭。⚠️ **二修（首版反而更糟，用户实测「无法滚动+提问框被顶出视口」）**：flex 高度链接错了根——flex 加在 `body` 上是死的，body 唯一子元素是 `#app`（index.html 挂载点），而 App.vue 的 `header/main/footer` 的父级是 `#app`（不是 flex 容器 → `main` 的 `flex:1/min-height:0` 全部失效 → main 高度=内容撑开 → 卡片不收缩 → 提问框被顶出 + body overflow:hidden 不让滚）。修正：`#app { height:100%; display:flex; flex-direction:column }` 作高度链的根，`main/.graph-wrap/.settings-wrap` 各自 `flex:1 + min-height:0`（图谱/设置页同步改为内容区内滚，整站不再整页滚动）。**教训：改布局先画「DOM 树 → 高度链」——flex 约束只作用于直接父容器，中间任何一层不是 flex/无定高，整条链断。**
4. **粘贴**：`.chat-zone` 加 `@paste`——`clipboardData.files` 有文件时 `preventDefault` 走 `pickFile`（不把图片当文本粘进 textarea）。

**验证**：后端 py_compile + 完整 flow 复现（reset→upload→chat：5 工具 + answer + 0 error）；前端 typecheck + build 全绿；「停止/指示/内滚/粘贴」为浏览器人工项（用户重启 dev 后按清单过）。

**教训**：① 「卡死」类 bug 先分层复现：后端正常 → 问题在「反馈缺失」而非「功能失效」——无提示窗口和无取消能力本身就是 bug；② 给长任务 SSE 流加「停止」是标准配置，AbortController + 服务端 GeneratorExit 天然贴合既有两遍循环历史完整性设计；③ flex 布局收缩三件套：父级固定高 + min-height:0 + overflow 交给最内层。

## 11. 会话历史三问：无保存/无查看/工具时长恒为 0（2026-08-29）

**现象**（用户实测三连）：① 没有保存历史会话的能力；② 工具调用时长总显示 0；③ 只能进新会话，无法查看历史会话。

**原因与解决**：
1. **①③ 多会话缺失**：此前方案把「多会话列表/切换 UI」列为排除项（单会话+「新会话」重置）。本次按用户新要求补齐：**会话 = 服务端 client_id**（sessions.json 本就按 id 落盘，后端零迁移）——前端新增 `useSessions.ts`（索引 `herb_agent_sessions` + 当前会话 `herb_agent_current` + 每会话分键 `herb_agent_units_<id>`/`herb_agent_stats_<id>` + 旧单会话键一次性迁移 + 与 `/api/sessions` 合并去重）；ChatPanel 顶部会话栏（下拉切换/＋新会话/🗑 删除 confirm）；后端补 `GET /api/sessions`（title=首条用户消息截 20 字、跳过系统注入）+ `DELETE /api/sessions/<id>`。设计要点：识别上下文/统计/上传文件全链路复用同一把 id 钥匙，天然按会话隔离；切会话清左卡识别结果。
2. **② 工具时长恒为 0**：计量无误（`perf_counter` 差值累加），是**显示口径**——直接量测各工具：图查询微秒级（check_compatibility ~0.02ms / get_herb_profile 2.3ms / similar_compare 0.05ms），向量检索秒级（retrieve_doc 首调 5.6s=模型加载、热调 12ms）；秒级四舍五入把亚秒全吞成 0。修复=三档显示 `<1ms / Nms / N.Ns`（`fmtDur`）。实测：一次 3 工具链总耗时 186µs——「工具链快慢由问题决定」。

**验证**：`GET /api/sessions` 200（4 会话含 title/msgs，删除后 3）；`DELETE` 200 幂等；chat 后 stats 原始 `tool_time=0.000062s`（真实微秒级）；前端 typecheck+build 全绿；py_compile 通过。UI 切换/新建/删除/重启恢复待用户 `npm run dev` 人工过。

**教训**：① 显示层 bug 先验证数据源再改 UI——这里如果先去查「是不是没累加」会绕远，直测各工具耗时立刻定位量级两极；② 「总为 0」≠「没测量」，亚秒值需要亚秒刻度。

## 12. 山楂图被答成「猫科动物」——旧拒识结论压过新识别结果（2026-08-29）

**现象**（用户实测）：先传过一张非药材图被拒后，再传山楂图片并问「这是啥」——左卡正确显示山楂（conf=1.0），但聊天回答「这张图片仍然不是中药饮片（疑似花草植物）」。

**原因**（后端完整复现 + sessions.json 逐条核对）：session 字段与注记全部正确（current_herb=山楂、upload_ctx=None、旧拒识注记已清、新山楂注记在位），元凶是**历史里残留的模型自己上一轮的拒识回复**——同样的问题「这是啥」再问一次，模型**自我一致性偏见**压过 system 注记，照抄旧结论。反向场景（先山楂后猫）更糟：模型照抄上一轮「根据【当前识别上下文】…识别结果为 X → 查档案」的**句式结构**，凭空编造不存在的「当前识别上下文=枸杞子」并真去调工具。

**解决**（软硬两层）：
1. 提示词层（防御纵深）：`_set_herb_context`/`_set_upload_context` 注记加「新旧图片身份区隔」（新上传≠旧图片、旧结论一律作废，包括模型自己说过的）；拒识注记加硬指令「禁止调药材工具/禁止展示档案/禁止编造识别结果」；BASE 底座加常设规则。**单独软约束实测不够**（B 方向仍编枸杞子）；
2. 硬修复（核心）：`app.py` 新增 `_reset_messages_for_new_upload()`——**新上传 = 新识别语境**，在 /upload 各判定分支写上下文前把消息历史整体切除（只留底座 system prompt；HERB_CTX/UPLOAD_CTX 由 stream_run 按最新判定重建）。文本-only 对话不经过 /upload 不受影响；同一张图生命周期内的多轮追问照常累积；tool_calls 随历史切除，无悬空问题。

**验证**（真实 DeepSeek，双向 ×2 轮）：A 猫→山楂→「这是啥」答山楂档案、零猫科词 ✅；B 山楂→猫→「这是啥」两次均正确拒识（非中药饮片/猫科动物+补拍建议）、零工具调用 ✅；同图多轮追问（功效）✅；重启后指代消解（山楂×菊花配伍）✅。

**教训**：① 模型对**自己历史回复**的自我一致性偏见 > system 注记的权威——上下文冲突时不能只靠提示词「说服」，要在**数据层**把过期结论删掉（截断历史），提示词只作纵深防御；② 模型的句式重复偏见会「按上一轮模板填空」——含工具调用的回答模板尤其危险（会带着编造的实体去执行工具）；③ 复现此类问题必做 sessions.json 逐条核对，先确认「注记都对、是旧回复在带偏」，否则会误判成清上下文逻辑 bug。
